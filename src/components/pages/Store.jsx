import React from "react";
import Default from "../templates/Default";

const Store = () => {
    return (
        <Default>
            <p>Contenu de la page Store</p>
        </Default>
    );
}

export default Store;